<div class="jumbotron text-center"><h1><%=@category.style%></h1></div>
<ul>
<%@products.each do |product|%>

<p class="p-3 text-primary"><li><%= link_to(image_tag(product.image), product_path(product)) %></li></p>
<%end%>
</ul>
<br>
<p class="p-3 text-primary"><%= link_to "My Account", user_path(session[:user_id])%></p>
<br>
<p class="p-3 text-primary"><%= link_to "Home", home_path%></p>