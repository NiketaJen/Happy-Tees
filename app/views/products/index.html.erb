<div class="jumbotron text-center"><h1>All Shirts (<%= Product.count %>)</h1></div>
<ul>
<%@products.each do |product|%>
<li><%= link_to image_tag(product.image), product_path(product)%></li>
<%end%>
</ul>
<%= link_to "Home", home_path %>
<br>
<p class="p-3 text-primary"><%= link_to "My Account", user_path(session[:user_id])%></p>
